
//observer
/*const observer = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.classList.add('aboutMe-animation')
    }
  })
});

observer.observe(document.querySelector('.aboutMe'));*/